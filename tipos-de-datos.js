// Datos Primitivos

//string  = cadena de caracteres

let nombre = "Tere";

//number = nos inidca un numero

let age = "27"

//boolean verdadero o falso

let EsMayorDeEdad = true;

//Null, un vacio como si no existiera, y nos ayudara para saber si hay algo o no, este si lo indicamos nosotros los programadores para definir si hay o no hay algo
let NoHayValor = null;

//undefined, tambien funciona como el null que no hay nada, no lo colcamos, esto no nos da javascript para indicarnos algo

let NoDefinido = undefined

//Symbol es un valor unico, como ID, no se utiliza mucho
let simboloUnico = Symbol('unico')

// biginit es para valores numerios muy grandes

let biginit = 2n;

//Tipo de datos complejos

//COMPLEJOS

//object, tenemos un objetos y tiene una caracteristicas propias

let carro = {
    marca: 'Toyota',
    color: 'Negra',
    modelo: '4X4',
    aceleracion:  '250km'
}

//arrays

let frutas = [ 'manzana', 'Banano',  'pera',  'uva']

//funciones = palabra clave seria function, ejemplo le ponemos la funcionalidad que es "saludar" y dentro de los (vamos a poner a quien vamos a saludar, su funcion en si) y en las llaves va el cuerpo de toda nuestra funcion

function saludar () {
    
}